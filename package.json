{
  "name": "quickstart",
  "type": "module",
  "scripts": {
    "update": "npm --workspaces run update && npm run postinstall",
    "postinstall": "node scripts/postinstall.ts",
    "dev": "dualist api web"
  },
  "devDependencies": {
    "dualist": "^0.0.7",
    "typescript": "^6.0.2"
  },
  "workspaces": [
    "api",
    "web"
  ]
}
